using Systems.PlanetScene;
using UnityEngine;
using Vectrosity;

namespace UI
{
    public class UITelescopeInterfacePanel : MonoBehaviour
    {
        public GameObject starPrefab;
        public GameObject unknownPrefab;
        public Transform parentStar;

        private VectorLine _fieldOfView;
        private TelescopeManager _telescope;

        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
